apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-rag-memory
  labels:
    app: rag-memory
  annotations:
    {{- toYaml .Values.annotations | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.apiPort }}
      targetPort: api
      protocol: TCP
      name: api
  selector:
    app: rag-memory
