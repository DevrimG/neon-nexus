image:
  repository: pontiffscopez/neon-nexus
  tag: "audio-latest"
  pullPolicy: Always

service:
  type: ClusterIP
  port: 8000

ingress:
  enabled: false

env: {}

annotations: {}

imagePullSecrets:
  - name: dockerhub-secret
